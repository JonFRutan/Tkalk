import sys

# This will define and implement "channels"
# Channels will be created by execution of the Server application, a "create new channel" should be added.
# Channels should have a whitelist of users, a password, a log of messages, an ID, and a name.
